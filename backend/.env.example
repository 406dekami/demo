# ===========================================
# 后端服务环境变量配置
# ===========================================

# -------------------------------------------
# DashScope API Key (必需)
# 用于通义千问模型调用（Embedding、生成等）
# 获取地址：https://dashscope.console.aliyun.com/
# -------------------------------------------
DASHSCOPE_API_KEY=sk-your-api-key-here

# -------------------------------------------
# Redis 配置 (可选，用于缓存知识图谱查询结果)
# -------------------------------------------
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# -------------------------------------------
# Neo4j 配置 (可选，用于知识图谱存储)
# -------------------------------------------
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-neo4j-password

# -------------------------------------------
# 数据库配置
# -------------------------------------------
DATABASE_PATH=storage/sqlite/demo.db

# -------------------------------------------
# 向量数据库配置
# -------------------------------------------
CHROMA_PERSIST_DIRECTORY=storage/chroma_db

# -------------------------------------------
# 文件上传配置
# -------------------------------------------
UPLOAD_DIR=data/uploads
MAX_FILE_SIZE=52428800  # 50MB

# -------------------------------------------
# 日志配置
# -------------------------------------------
LOG_LEVEL=INFO
LOG_DIR=logs

# -------------------------------------------
# CORS 配置 (生产环境应限制具体域名)
# -------------------------------------------
CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]

# -------------------------------------------
# 数据初始化配置
# -------------------------------------------
AUTO_INIT_SEED_DATA=true  # 启动时自动初始化种子数据（思维导图、知识图谱）
FORCE_REINIT=false        # 强制重新初始化（清空现有数据后重新导入）
