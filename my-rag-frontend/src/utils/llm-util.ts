/**
 * LLM 工具函数
 */

/**
 * 获取模型的真实名称
 * @param llmName - 模型的内部名称
 * @returns 真实显示名称
 */
export const getRealModelName = (llmName: string): string => {
  // 这里可以根据需要添加实际的名称映射逻辑
  // 目前直接返回原名称
  return llmName;
};
